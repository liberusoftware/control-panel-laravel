<section aria-labelledby="mail-inventory-heading"><h2 id="mail-inventory-heading">Mail accounts</h2>
    <label for="mail-search">Search mail accounts</label><input id="mail-search" type="search" wire:model.live.debounce.300ms="search"><ul>@forelse($items as $item)<li wire:key="mail-account-{{ $item->getKey() }}"><form wire:submit="update('{{ $item->getKey() }}', null)"><input aria-label="{{ __('Mail domain') }}" wire:model="edits.{{ $item->getKey() }}.domain" value="{{ $item->domain }}"><input aria-label="{{ __('Mail address') }}" wire:model="edits.{{ $item->getKey() }}.address" value="{{ $item->address }}"><input aria-label="{{ __('Mailbox quota') }}" type="number" wire:model="edits.{{ $item->getKey() }}.quota_bytes" value="{{ $item->quota_bytes }}"><button type="submit">{{ __('Save') }}</button></form>{{ $item->status }}</li>@empty<li>No mail accounts found.</li>@endforelse</ul></section>
