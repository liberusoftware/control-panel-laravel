<?php
declare(strict_types=1); use Illuminate\Database\Migrations\Migration; use Illuminate\Database\Schema\Blueprint; use Illuminate\Support\Facades\Schema;
return new class() extends Migration { public function up():void { Schema::create('control_panel_certificate_deployments',function(Blueprint $t):void{$t->uuid('id')->primary();$t->string('team_id')->nullable()->index();$t->uuid('certificate_id');$t->string('target_type');$t->string('target_id');$t->string('status')->default('queued');$t->timestamp('deployed_at')->nullable();$t->text('error')->nullable();$t->json('metadata')->nullable();$t->timestamps();}); Schema::create('control_panel_certificate_renewals',function(Blueprint $t):void{$t->uuid('id')->primary();$t->string('team_id')->nullable()->index();$t->uuid('certificate_id');$t->timestamp('scheduled_at')->nullable();$t->timestamp('started_at')->nullable();$t->timestamp('completed_at')->nullable();$t->string('status')->default('queued');$t->unsignedTinyInteger('attempts')->default(0);$t->text('error')->nullable();$t->timestamps();}); Schema::create('control_panel_certificate_expiry_alerts',function(Blueprint $t):void{$t->uuid('id')->primary();$t->string('team_id')->nullable()->index();$t->uuid('certificate_id');$t->unsignedInteger('threshold_days')->default(30);$t->string('status')->default('pending');$t->timestamp('notified_at')->nullable();$t->json('metadata')->nullable();$t->timestamps();}); }
 public function down():void{Schema::dropIfExists('control_panel_certificate_expiry_alerts');Schema::dropIfExists('control_panel_certificate_renewals');Schema::dropIfExists('control_panel_certificate_deployments');} };
