<section aria-labelledby="backup-schedule-inventory-heading"><h2 id="backup-schedule-inventory-heading">{{ __('Backup schedules') }}</h2><ul>@forelse($schedules as $schedule)<li wire:key="backup-schedule-{{ $schedule->getKey() }}"><form wire:submit="updateSchedule('{{ $schedule->getKey() }}', null)" class="inline"><input aria-label="{{ __('Cron schedule') }}" wire:model="scheduleEdits.{{ $schedule->getKey() }}.cron" value="{{ $schedule->cron }}"><input aria-label="{{ __('Schedule timezone') }}" wire:model="scheduleEdits.{{ $schedule->getKey() }}.timezone" value="{{ $schedule->timezone }}"><button type="submit">{{ __('Save') }}</button></form> <button type="button" wire:click="deleteSchedule('{{ $schedule->getKey() }}')">{{ __('Delete') }}</button></li>@empty<li>{{ __('No backup schedules found.') }}</li>@endforelse</ul>{{ $schedules->links() }}</section>
