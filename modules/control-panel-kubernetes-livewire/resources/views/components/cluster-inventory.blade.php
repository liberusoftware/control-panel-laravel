<section aria-labelledby="cluster-inventory-heading"><h2 id="cluster-inventory-heading">Kubernetes clusters</h2><ul>@forelse($items as $item)<li wire:key="cluster-{{ $item->getKey() }}">{{ $item->name }} — {{ $item->status }} @if ($item->status !== 'suspended' && $item->status !== 'archived')<button type="button" wire:click="suspend('{{ $item->getKey() }}')" wire:loading.attr="disabled">Suspend</button>@endif @if ($item->status !== 'archived')<button type="button" wire:click="archive('{{ $item->getKey() }}')" wire:loading.attr="disabled">Archive</button>@endif</li>@empty<li>No clusters found.</li>@endforelse</ul></section>
