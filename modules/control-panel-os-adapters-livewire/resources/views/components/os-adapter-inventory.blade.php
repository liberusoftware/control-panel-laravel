<section aria-labelledby="os-adapter-inventory-heading"><h2 id="os-adapter-inventory-heading">OS adapters</h2><ul>@forelse($items as $item)<li wire:key="adapter-{{ $item->getKey() }}">{{ $item->operating_system }} {{ $item->version }} — {{ $item->status }}</li>@empty<li>No OS adapters found.</li>@endforelse</ul><h2>Support matrix</h2><ul>@forelse($supportMatrix as $entry)<li wire:key="support-{{ $entry->getKey() }}">{{ $entry->operating_system }} {{ $entry->version }} — {{ $entry->capability }}: {{ $entry->supported ? 'supported' : 'unsupported' }}</li>@empty<li>No support matrix entries found.</li>@endforelse</ul></section>
