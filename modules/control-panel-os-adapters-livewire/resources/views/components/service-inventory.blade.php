<section aria-labelledby="os-service-inventory-heading"><h2 id="os-service-inventory-heading">{{ __('Services') }}</h2><ul>@forelse($services as $service)<li wire:key="os-service-{{ $service->getKey() }}"><form wire:submit="update('{{ $service->getKey() }}', null)"><input type="text" wire:model="edits.{{ $service->getKey() }}.name" value="{{ $service->name }}" maxlength="160" required><button type="submit">{{ __('Save') }}</button></form><span>{{ $service->status }}</span></li>@empty<li>{{ __('No services found.') }}</li>@endforelse</ul>{{ $services->links() }}</section>
