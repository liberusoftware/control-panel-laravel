<section aria-labelledby="finding-inventory-heading"><h2 id="finding-inventory-heading">Security findings</h2>
    <label for="finding-search">Search findings</label><input id="finding-search" type="search" wire:model.live.debounce.300ms="search"><ul>@forelse($items as $item)<li wire:key="security-finding-{{ $item->getKey() }}">{{ $item->summary }} — {{ $item->severity }} @if($item->status === 'open') <button type="button" wire:click="resolve('{{ $item->getKey() }}')">Resolve</button> @endif</li>@empty<li>No security findings found.</li>@endforelse</ul>{{ $items->links() }}</section>
