<section aria-labelledby="compliance-inventory-heading"><h2 id="compliance-inventory-heading">{{ __('Compliance status') }}</h2><ul>@forelse($statuses as $status)<li wire:key="compliance-{{ $status->getKey() }}">{{ $status->framework }} / {{ $status->control }} — {{ $status->status }} ({{ $status->score ?? '—' }})</li>@empty<li>{{ __('No compliance statuses found.') }}</li>@endforelse</ul>{{ $statuses->links() }}</section>
