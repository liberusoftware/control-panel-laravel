# Control Panel OS Adapters

Provider-neutral operating-system adapter inventory. Execution belongs to separately reviewed adapters.

The read-only installation-command report suggests package-manager commands for known missing
services based on the recorded adapter operating system. It never executes commands; unknown
operating systems and service names produce no suggestion.
